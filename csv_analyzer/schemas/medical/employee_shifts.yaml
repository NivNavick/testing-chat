name: employee_shifts
vertical: medical
version: "1.0"
description: "Employee work schedules, shift assignments, staff rosters, nursing schedules"

fields:
  - name: shift_id
    type: string
    required: false
    description: "Unique identifier for this shift record or schedule entry"
    embedding_text: "shift record schedule unique identifier ID number"

  - name: employee_id
    type: string
    required: true
    description: "Unique identifier for the employee, staff member, or healthcare worker"
    aliases:
      # English variations
      - emp_id
      - staff_id
      - worker_id
      - emp_num
      - employee_number
      - staff_code
      - worker_code
      - nrs_id
      - nurse_id
      - personnel_id
      # Hebrew (עברית)
      - מספר_עובד
      - קוד_עובד
      - מזהה_עובד
      # Spanish (Español)
      - numero_empleado
      - id_empleado
      - codigo_trabajador
      # Arabic (العربية)
      - رقم_الموظف
      - معرف_العامل
      # Russian (Русский)
      - номер_сотрудника
      - id_работника
      # French (Français)
      - numero_employe
      - id_employe
    embedding_text: "employee worker staff nurse identifier ID number code unique personnel trabajador employé сотрудник עובד موظف"

  - name: shift_date
    type: date
    required: true
    description: "The calendar date when the shift occurs"
    aliases:
      - work_date
      - schedule_date
      - date
      - shift_dt
      - dt
      - work_dt
      - working_date
      # Hebrew
      - תאריך_משמרת
      - תאריך_עבודה
      - תאריך
      # Spanish
      - fecha_turno
      - fecha_trabajo
      # Arabic
      - تاريخ_الوردية
      # Russian
      - дата_смены
      # French
      - date_travail
    embedding_text: "shift work schedule date day calendar when working fecha תאריך дата تاريخ"

  - name: shift_start
    type: datetime
    required: true
    description: "The time when the shift begins, clock in time"
    aliases:
      - start_time
      - clock_in
      - time_in
      - begin
      - start
      - in_time
      - t_in
      - entry_time
      # Hebrew
      - שעת_התחלה
      - שעת_כניסה
      - התחלה
      - כניסה
      # Spanish
      - hora_entrada
      - inicio_turno
      # Arabic
      - وقت_البداية
      # Russian
      - время_начала
      # French
      - heure_debut
    embedding_text: "shift start begin clock in time entry arrival punch entrada início начало بداية התחלה"

  - name: shift_end
    type: datetime
    required: true
    description: "The time when the shift ends, clock out time"
    aliases:
      - end_time
      - clock_out
      - time_out
      - finish
      - end
      - out_time
      - t_out
      - exit_time
      # Hebrew
      - שעת_סיום
      - שעת_יציאה
      - סיום
      - יציאה
      # Spanish
      - hora_salida
      - fin_turno
      # Arabic
      - وقت_النهاية
      # Russian
      - время_окончания
      # French
      - heure_fin
    embedding_text: "shift end finish clock out time departure punch leave salida fim окончание نهاية סיום"

  - name: duration_minutes
    type: integer
    required: false
    description: "Total duration of the shift in minutes or hours worked"
    aliases:
      - duration
      - hours_worked
      - hrs
      - work_hours
      - shift_length
      - dur_hrs
      - hours
      - total_hours
      # Hebrew
      - משך_משמרת
      - שעות_עבודה
      - משך
      # Spanish
      - horas_trabajadas
      - duracion
      # Arabic
      - ساعات_العمل
      # Russian
      - часы_работы
      # French
      - heures_travaillees
    embedding_text: "duration hours worked length time total shift period horas שעות часы ساعات heures"

  - name: department_code
    type: string
    required: false
    description: "Department, unit, or ward where the shift takes place"
    aliases:
      - department
      - dept
      - dept_code
      - unit
      - unit_code
      - ward
      - location
      - dept_cd
      - section
      # Hebrew
      - מחלקה
      - יחידה
      - שם_מחלקה
      - קוד_מחלקה
      # Spanish
      - departamento
      - unidad
      # Arabic
      - القسم
      - الوحدة
      # Russian
      - отделение
      - подразделение
      # French
      - departement
      - service
    embedding_text: "department unit ward section location area hospital clinic departamento מחלקה отдел قسم"

  - name: shift_type
    type: string
    required: false
    description: "Type of shift - morning, evening, night, day, overnight"
    aliases:
      - type
      - shift_kind
      - shift_category
      - shift_name
      # Hebrew
      - סוג_משמרת
      - סוג
      # Spanish
      - tipo_turno
      # Arabic
      - نوع_الوردية
      # Russian
      - тип_смены
      # French
      - type_poste
    embedding_text: "shift type morning evening night day overnight category kind turno סוג тип نوع"
