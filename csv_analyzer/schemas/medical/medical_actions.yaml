name: medical_actions
vertical: medical
version: "1.0"
description: "Medical procedures, treatments, clinical actions, healthcare services performed on patients"

fields:
  - name: action_id
    type: string
    required: true
    description: "Unique identifier for the medical action, procedure, or service"
    aliases:
      - procedure_id
      - service_id
      - act_id
      - id
      - record_id
      - treatment_id
      - encounter_id
      # Hebrew
      - מזהה_פעולה
      - מזהה_טיפול
      - מספר_פעולה
      # Spanish
      - id_procedimiento
      - id_accion
      # Arabic
      - معرف_الاجراء
      # Russian
      - id_процедуры
      # French
      - id_acte
    embedding_text: "action procedure service treatment identifier ID number unique record procedimiento פעולה процедура إجراء"

  - name: patient_id
    type: string
    required: true
    description: "Unique identifier for the patient receiving the treatment"
    aliases:
      - patient
      - patient_mrn
      - mrn
      - pt_id
      - patient_num
      - patient_number
      - medical_record
      # Hebrew
      - מספר_מטופל
      - מזהה_מטופל
      - מטופל
      # Spanish
      - id_paciente
      - numero_paciente
      # Arabic
      - رقم_المريض
      # Russian
      - id_пациента
      # French
      - id_patient
    embedding_text: "patient person medical record number MRN identifier recipient paciente מטופל пациент مريض"

  - name: performer_id
    type: string
    required: true
    description: "Identifier for the healthcare provider who performed the action"
    aliases:
      - physician_id
      - doctor_id
      - provider_id
      - prov_id
      - staff_id
      - practitioner_id
      - clinician_id
      # Hebrew
      - מזהה_רופא
      - מזהה_מטפל
      - קוד_רופא
      # Spanish
      - id_medico
      - id_proveedor
      # Arabic
      - معرف_الطبيب
      # Russian
      - id_врача
      # French
      - id_medecin
    embedding_text: "performer provider physician doctor nurse practitioner healthcare worker staff medico רופא врач طبيب"

  - name: action_code
    type: string
    required: true
    description: "Procedure or service code (CPT, ICD-10-PCS, SNOMED, local codes)"
    aliases:
      - procedure_code
      - cpt_code
      - code
      - service_code
      - treatment_code
      - billing_code
      # Hebrew
      - קוד_פעולה
      - קוד_טיפול
      - קוד
      # Spanish
      - codigo_procedimiento
      - codigo
      # Arabic
      - رمز_الاجراء
      # Russian
      - код_процедуры
      # French
      - code_acte
    embedding_text: "procedure code CPT ICD SNOMED service treatment billing codigo קוד код رمز"

  - name: action_name
    type: string
    required: false
    description: "Human readable name or description of the procedure"
    aliases:
      - procedure_name
      - procedure_desc
      - description
      - name
      - service_name
      - treatment_name
      - action_description
      # Hebrew
      - שם_פעולה
      - תיאור_פעולה
      - תיאור
      # Spanish
      - nombre_procedimiento
      - descripcion
      # Arabic
      - اسم_الاجراء
      # Russian
      - название_процедуры
      # French
      - nom_acte
    embedding_text: "procedure name description title action service treatment what nombre שם название اسم"

  - name: performed_at
    type: datetime
    required: true
    description: "Date and time when the action was performed"
    aliases:
      - action_date
      - service_datetime
      - datetime
      - timestamp
      - date
      - service_date
      - procedure_date
      - performed_date
      # Hebrew
      - תאריך_ביצוע
      - זמן_ביצוע
      - תאריך_פעולה
      # Spanish
      - fecha_procedimiento
      - fecha_hora
      # Arabic
      - تاريخ_التنفيذ
      # Russian
      - дата_выполнения
      # French
      - date_execution
    embedding_text: "performed date time when action service timestamp fecha תאריך дата تاريخ"

  - name: department_code
    type: string
    required: false
    description: "Department or service location where action was performed"
    aliases:
      - department
      - dept
      - service_dept
      - location
      - clinic
      - unit
      - facility
      # Hebrew
      - מחלקה
      - מחלקה_מבצעת
      - יחידה
      # Spanish
      - departamento
      - ubicacion
      # Arabic
      - القسم
      # Russian
      - отделение
      # French
      - departement
    embedding_text: "department location clinic unit service area ward departamento מחלקה отдел قسم"

  - name: diagnosis_code
    type: string
    required: false
    description: "Related diagnosis code (ICD-10, ICD-9)"
    aliases:
      - icd10_code
      - icd_code
      - dx
      - diagnosis
      - dx_code
      - diagnostic_code
      # Hebrew
      - קוד_אבחנה
      - אבחנה
      # Spanish
      - codigo_diagnostico
      - diagnostico
      # Arabic
      - رمز_التشخيص
      # Russian
      - код_диагноза
      # French
      - code_diagnostic
    embedding_text: "diagnosis ICD code condition reason indication diagnostico אבחנה диагноз تشخيص"

  - name: notes
    type: string
    required: false
    description: "Additional notes or comments about the action"
    aliases:
      - comments
      - remarks
      - note
      - observation
      # Hebrew
      - הערות
      - הערה
      # Spanish
      - notas
      - observaciones
      # Arabic
      - ملاحظات
      # Russian
      - примечания
      # French
      - remarques
    embedding_text: "notes comments remarks additional information notas הערות примечания ملاحظات"
